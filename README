Configure and build with:

    $ ./configure CFLAGS='-g3 -O0' CXXFLAGS='-g3 -O0'
    $ make

Change simulation params:

    $ vi src/lustre.cpp

Run with:

    $ mkdir run
    $ cd run
    $ ../src/lustre
